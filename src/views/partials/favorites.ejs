<div ng-app="myApp" ng-controller="favoritesCtrl">
  <h1>My Favorites Movies</h1>
  <li ng-repeat="movie in movies">
    {{movie.Title}} <button ng-click="toggleFavorites(movie)">+</button>
  </li>
</div>
